let init = {
  posts: [
    {
      title: "Test Post",
      id: "23232",
    },
  ],
  fetchedPosts: [],
};

const postsReducer = (state = init, action) => {
  return state;
};

export default postsReducer;
